# dsh-desktop 产品改进建议（调整版）

> 本文件覆盖原对标稿，作为后续产品与实现路线的唯一参考。
> 调整日期：2026-08-20。

## 一、产品定位

dsh-desktop 不应通过堆叠 Provider、插件市场或跨平台外壳与通用 AI 客户端竞争。它的核心定位是：

**DeepSeek Harness 的 macOS 原生控制面，以及 Guardian 安全运行链的唯一桌面所有者。**

项目的差异化由两部分共同组成：

- macOS 原生常驻体验：全局唤起、系统通知、菜单栏、原生审批与快速回到任务；
- Guardian 安全能力：候选配置隔离预检、last-known-good、失败恢复、安全模式、变更差异与显式回滚。

Guardian 的源码、安装生命周期和恢复操作只归 dsh-desktop 所有。dsh-ops-console 是可选的 Web/移动运维客户端，不是桌面功能的运行依赖，也不得在 Guardian 缺失时绕过保护直接重启。

## 二、实施原则

1. **先建立正式事件协议，再做通知和浮窗。** 不监听日志猜状态，不抓取或模拟点击网页 DOM。
2. **所有可变更操作都要失败关闭。** 审批、配置恢复和重启必须有明确能力检查、用户动作和可审计结果。
3. **保持仓库解耦。** dsh-desktop 只依赖 Harness/Guardian 的版本化协议，不导入 dsh-ops-console 的源码、路径或私有接口。
4. **原生端不复制 Harness 已有领域逻辑。** 模型路由、会话持久化、插件目录仍由 Harness/插件层负责；桌面端只提供安全、快捷的控制面。
5. **隐私优先。** 事件桥只监听回环地址，使用本地令牌认证；通知不默认展示敏感命令全文。

## 三、路线图

### P0：把客户端变成可信的原生控制面

> 实施状态（2026-08-20）：四项功能均已进入 dsh-desktop 0.4.0。协议与 Guardian 自动验证通过；真实环境已验证令牌拒绝、畸形请求拒绝、事件去重、快速提问、客户端退出后端口释放、晚启动自动重连以及 Guardian 原生状态/差异面板。系统通知和原生审批仍以 macOS 通知授权为前提；未授权时已验证会失败关闭并回退 Harness Web。

#### 1. 本地事件桥接

- 定义稳定事件：`task.started`、`task.progress`、`approval.requested`、`task.completed`、`task.failed`、`guardian.preflight`、`guardian.recovered`、`balance.low`。
- 仅绑定 `127.0.0.1`，请求必须携带安装时生成的本地令牌。
- 事件可携带本地回调地址；只允许 `http://127.0.0.1` 或 `http://localhost`，禁止向任意远端转发审批结果。
- 桥接层与 Harness 通过协议集成，不读取 dsh-ops-console。

验收：无令牌请求被拒绝；重复事件不会产生重复通知；客户端退出后端口释放；畸形事件不会导致崩溃。

#### 2. 原生通知与审批

- 使用 `UNUserNotificationCenter` 展示审批、完成、失败、Guardian 恢复和低余额通知。
- 审批通知提供“允许一次 / 拒绝 / 打开任务”；通知操作通过事件声明的本地回调完成。
- 若回调不存在、超时或协议不兼容，只打开客户端，不做推测性审批。
- 通知正文默认使用摘要；命令全文只在客户端内查看。

验收：允许和拒绝均能到达本地回调；失败时明确提示且保持待审批；普通完成通知不能触发审批动作。

#### 3. 全局快捷键与迷你提问面板

- 默认快捷键 `⌥Space`，弹出原生 `NSPanel`，支持输入、取消和打开完整客户端。
- 迷你面板将问题发送给已连接的 Harness 提问端点；未连接时保留草稿并打开主窗口，不能假装发送成功。
- 快捷键注册失败时在菜单中仍可打开，并给出可诊断状态。

验收：前台或后台均可唤起；Escape 关闭；发送状态可见；服务不可用时不丢失输入。

#### 4. Guardian 配置差异与回滚回执

- Guardian 提供机器可读的 `diff` 能力，比较当前 profile/受保护集成与 last-known-good。
- 桌面保护面板展示新增、修改、删除和无法读取的项目，不显示密钥正文。
- “恢复黄金版本”继续由 Guardian 执行；完成后展示恢复时间和变更回执。

验收：无黄金版本时安全返回；只改一个受保护文件时能准确识别；恢复后差异归零；路径不会逃逸 `DSH_HOME`。

### P1：安全变更与数据洞察

- 将插件/MCP 管理改造为“安全变更中心”：预览差异 → Guardian 隔离预检 → 用户确认 → 应用或回滚；不重建原生插件市场。
- 基于 Harness 的真实 token/费用遥测提供会话成本、预算预警和趋势；数据不足时不得用余额变化伪造精确成本。
- 会话搜索优先复用 Harness 查询能力或独立本地索引服务，不在 Swift 端复制会话数据库。
- 值守伙伴属于独立可选项目，当前暂不实施。

### P2：macOS 深度集成

- Finder Quick Action、Shortcuts、语音输入和朗读。
- Provider 设置仅作为 Harness 路由/插件能力的安全配置界面；客户端本身不实现模型路由、重试和降级引擎。

### 暂缓事项

- 暂不跨平台重写。当前优先发挥 Swift/AppKit 在通知、全局面板、菜单栏和系统集成上的优势。
- 暂不增加纯动画桌宠、AI 屏保或与真实运行状态无关的装饰功能。
- 暂不重新引入客户端内置插件市场。

## 四、P0 技术边界

事件桥协议必须版本化。P0 使用 `protocolVersion: 1`，事件至少包含：

```json
{
  "protocolVersion": 1,
  "id": "stable-event-id",
  "type": "approval.requested",
  "title": "需要批准命令",
  "message": "任务正在等待你的决定",
  "sessionId": "optional-session-id",
  "callbackURL": "http://127.0.0.1:3080/versioned/local-callback"
}
```

安全边界：

- 桥接令牌保存在用户目录，权限为 `0600`；
- 服务只接受有限大小的 JSON 请求；
- 回调只允许回环地址，且审批结果使用固定枚举 `allowed-once` / `rejected`；
- 事件 ID 用于短期去重；
- Guardian 的 diff 只返回路径、状态和摘要，不返回配置内容或秘密值。

## 五、后续优先级

P0 完成后，应先观察通知提交成功率、审批回调成功率、全局面板使用率以及 Guardian 回滚触发原因，再决定 P1。dsh-desktop 0.4.0 已加入本机 `native-metrics.json` 计数和 `dshctl native-metrics` 查询；统计不得记录命令、提问内容、配置正文、密钥或会话标识。功能数量和竞品清单不作为优先级依据。

一句话方向：

**让 dsh-desktop 成为随时可唤起、能准确告知状态、能安全完成审批与恢复的 macOS 原生 Harness 控制面。**
