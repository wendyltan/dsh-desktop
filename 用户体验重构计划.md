# dsh 用户体验重构计划

更新日期：2026-08-22  
适用范围：`dsh-desktop` 与 `dsh-ops-console`

## 1. 这次重构要解决什么

当前界面把 Guardian、事件桥、协议、黄金版本、快照等实现概念直接交给用户判断。用户真正关心的却是：

1. DeepSeek Harness 现在能不能用？
2. 如果不能用，受影响的是哪里？
3. 我现在应该做什么？
4. 系统是否已经在处理，是否会保护我原来的可用状态？

本次重构的目标是：默认界面只回答这四个问题；技术信息只在“技术详情”或“生成诊断报告”中按需出现。

## 2. 产品边界

| 产品 | 对用户的职责 | 不承担的职责 |
| --- | --- | --- |
| dsh-desktop | 在本机打开 Harness、检查/安装更新、在故障时让服务恢复可用 | 远程协作、设备配对、通用运维面板 |
| Guardian | dsh-desktop 内部使用的安全启动、检查和恢复实现 | 用户界面、插件功能、远程控制中心 |
| dsh-ops-console | 在浏览器/手机上说明当前状态、引导诊断、展示经过脱敏的诊断证据 | 引擎更新、Guardian 控制、插件市场、通用聊天 |

禁止新增以下耦合：

- dsh-desktop 不能请求 `/dsh-ops/*` 才能工作。
- dsh-ops 不能负责安装、升级、恢复或控制 Guardian；Guardian 缺失时它仍是完整的状态/诊断工具。
- Guardian 不得识别或硬编码 dsh-ops 的路由、名称或部署路径。
- 可选信息源缺失时，界面写“未启用额外检查”，不能把整体判为故障。

## 3. 统一信息表达规范

### 3.1 状态词

全产品只使用四类面向用户的状态：

| 状态 | 含义 | 典型下一步 |
| --- | --- | --- |
| 正常 | 当前可继续使用 | 无；可查看详情 |
| 需要检查 | 发现异常但原因尚未确认 | 检查问题 |
| 正在处理 | 正在检查、更新或恢复 | 等待；查看进度 |
| 需要你决定 | 需要用户确认有影响的操作 | 查看影响并确认 |

不得在主界面使用“黄金版本”“安全模式”“安全重启”“预检”“事件桥”“协议”“模块”“快照”“boot manifest”“PID”“端口”等术语。

### 3.2 每张状态卡的固定顺序

1. 一句结论。
2. 一句影响说明。
3. 一个主按钮。
4. 正在处理时的自然语言进度。
5. 可选的“技术详情”。

错误不能只显示原始错误。必须先说明是否仍可使用、是否已自动恢复、以及用户能否安全重试。

### 3.3 风险操作文案

每个会改变运行状态的操作都说明三件事：会发生什么、短暂影响什么、失败时如何保护原状。

示例：

> 重新启动服务
>
> 服务会短暂重新连接。开始前会检查新状态；如果无法正常启动，会继续使用此前能正常运行的状态。

实现概念可以在技术详情中以解释形式出现，例如“上一次正常版本（用于更新失败时自动恢复）”。

## 4. dsh-desktop 设计

### 4.1 菜单栏

默认菜单：

1. `打开 DeepSeek Harness`
2. `当前状态…`
3. `检查更新`
4. `帮助与诊断…`
5. `设置…`

仅有可用更新时，将第三项改为 `可更新到 <版本>…`。不在菜单栏展示 Guardian、余额或内部版本链。

### 4.2 当前状态窗口

窗口名称：`当前状态`。默认只显示一张状态卡。

| 情形 | 主标题 | 说明 | 主操作 |
| --- | --- | --- | --- |
| 正常 | DeepSeek Harness 正常运行 | 你可以继续使用。上次检查：刚刚。 | 打开 DeepSeek Harness |
| 未连接 | DeepSeek Harness 暂时无法连接 | 网页和远程访问可能无法使用。 | 检查并尝试恢复 |
| 自动处理中 | 正在恢复服务 | 正在检查并恢复可用状态。完成后会自动再次确认。 | 查看进度 |
| 已恢复 | 服务已恢复 | 已回到可以正常使用的状态。 | 打开 DeepSeek Harness |
| 需要人工决定 | 发现可用更新 | 更新前会检查能否正常启动；失败时保留当前可用状态。 | 查看更新 |

正常状态不得显示模块数量、进程号、快照数量、配置差异、原生事件计数或原始错误。

窗口底部保留 `技术详情` 折叠区与 `生成诊断报告`。技术详情才显示 Guardian 版本、运行模式、最后错误、配置差异和原生事件。

### 4.3 更新窗口

更新按钮进入专用说明页，步骤为：

1. 下载更新。
2. 检查能否正常启动。
3. 应用更新。
4. 确认 DeepSeek Harness 可用。

失败结果必须写：`更新没有完成，但此前能正常运行的版本仍在使用。` 并提供 `查看技术详情` 和 `生成诊断报告`。

## 5. dsh-ops-console 设计

### 5.1 定位与首页

定位：`遇到问题时，告诉我发生了什么，并带我完成下一步。`

首页名称：`现在怎么样`。第一屏只有总状态、影响说明、一个操作和最近变化；不能出现引擎/Guardian/协议/日志尾部/可信主机等卡片。

| 状态 | 首页文案 | 操作 |
| --- | --- | --- |
| 正常 | 现在可以正常使用。你不需要做任何事。 | 检查问题 |
| 本机异常 | DeepSeek Harness 暂时无法连接。网页功能可能不可用。 | 检查问题 |
| 远程异常 | 本机可以使用，但另一台设备暂时连不上。 | 检查远程访问 |
| 检查中 | 正在检查问题。通常不需要手动操作。 | 查看进度 |

### 5.2 页面与任务

| 页面 | 用户为何进入 | 默认内容 | 主操作 |
| --- | --- | --- | --- |
| 现在怎么样 | 想确认是否可用 | 结论、影响、最近变化 | 检查问题 |
| 检查问题 | 网页打不开、历史加载失败、远程连不上 | 选择现象、自动检查、自然语言结果 | 根据结果继续 |
| 发生了什么 | 想知道问题缘由 | 自然语言时间线 | 查看详情 / 生成报告 |
| 在另一台设备上打开 | 想从手机或另一台电脑查看 | 配对/访问说明、授权设备列表 | 开始配对 / 撤销设备 |
| 设置 | 调整通知和隐私 | 少量用户设置 | 保存 |

“服务器”“远程访问”“日志”“Guardian”“余额”等技术分组不再作为一级导航。余额只在用户主动打开“账户与用量”卡片时出现；原始日志仅在诊断结果的技术详情中出现。

### 5.3 检查问题向导

先让用户选择看得懂的现象：

- 网页打不开或一直转圈。
- 历史记录加载失败。
- 手机/另一台电脑打不开。
- 使用时变慢或经常断开。
- 其他问题。

检查结果必须使用“结论 → 影响 → 下一步”的格式。例如：

> 本机服务正常，但远程入口未连通。
>
> 这不会影响在这台 Mac 上使用，但手机或外部网络暂时打不开。
>
> `重新检查远程访问`  `生成诊断报告`

### 5.4 时间线与诊断报告

时间线只记录用户可理解的事件，例如“远程连接中断”“服务已恢复”“检查完成”。原始事件名、错误码和日志放进每项的技术详情。

诊断报告默认脱敏：不得包含密钥、提示词、会话正文、完整配置、令牌或可直接识别个人的远程地址。报告用于支持时包含版本、检查结论、时间线和经过裁剪的错误摘要。

### 5.5 远程设备

远程设备页面使用短时配对码/二维码、设备名、有效期和撤销操作。它必须由 dsh-ops 自己的认证机制提供，不能依赖桌面端或 Guardian。该能力在本轮只规划接口和页面占位，不在未完成安全设计前开放。

## 6. 必须删除或降级的内容

- dsh-ops 的引擎最新版本检测、更新、恢复和 Guardian 控制。
- 默认页的服务细节、日志尾部、版本链、配置差异和指标计数。
- 以“安全重启”“黄金版本”等内部名词命名的按钮。
- 任何要求普通用户先理解运行架构才能继续的表单。

## 7. 实施顺序与验收

### P0：术语、层级与状态卡

- [ ] 编写并链接本计划和两个仓库的待办清单。
- [ ] dsh-desktop 将“服务保护”改为“当前状态”，默认折叠技术详情。
- [ ] dsh-desktop 更新进度和失败文案改为四步用户流程。
- [ ] dsh-ops 导航、首页和 README 改为任务式语言。
- [ ] 两端清理默认可见的内部术语。

验收：新用户在不阅读文档的情况下能回答“现在能不能用”和“下一步按哪里”。

### P1：指导式诊断

- [ ] dsh-ops 实现按症状开始的检查问题页。
- [ ] dsh-ops 将已有检查信号转换为自然语言结论。
- [ ] dsh-ops 增加“发生了什么”时间线与折叠技术详情。
- [ ] 两端提供默认脱敏诊断报告入口。

验收：遇到远程或本机异常时，用户无需认识端口、日志或 Guardian，也能获得下一步。

### P2：远程协作与完善

- [ ] 设计并审计独立的短时配对/撤销协议。
- [ ] 增加必要时才发送的通知。
- [ ] 在高级视图保留支持排障需要的内部信息。

验收：不安装 dsh-desktop 或 Guardian 的 dsh-ops 仍可独立完成状态说明与诊断；不安装 dsh-ops 的 dsh-desktop 仍可独立更新和恢复。

## 8. 这轮交付边界

本轮实施 P0，并落地 P1 所需的数据模型和页面骨架；任何会改变远程授权边界的 P2 配对功能均不直接上线。每个仓库必须独立构建和测试，部署前再单独申请/执行相应的安装或重启步骤。
